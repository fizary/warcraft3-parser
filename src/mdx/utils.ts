// @NOTE: All ID's (objectId, parentId, globalSequenceId, etc...) can use the special value "0xFFFFFFFF" as "no ID"
// Some ID's can use it to represent "multiple IDs"
export const NoID = 0xFFFFFFFF;
